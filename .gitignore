.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.DS_Store
# The host lock lives in Roost's own state directory (see roost/paths.py), not
# here — this only catches one left behind by an older build that kept it in the
# working tree.
*.lock
.pytest_cache/
.ruff_cache/
.agents/
.claude/
